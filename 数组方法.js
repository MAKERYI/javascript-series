let arr = [1, 3, 4, 3]

// Array.isArray()
function check(arr) {
  // return typeof arr
  // return Array.isArray(arr)
  // return arr.valueOf()
  // return arr.toString()
  // return arr.push(1)
  // return arr.pop()
  // return arr.shift()
  // return arr.unshift(1)
  // return arr.join('-')
  // return arr.concat([11], [3])
  // return arr.reverse()
  // return arr.slice(1,3)
  // return arr.splice(1, 2, 5, 6)
  // return arr.splice(1, 0, 4)
  // return arr.splice(2)
  // return arr.sort((a, b) => a - b)
  // return arr.sort((a, b) => b - a)
  // return arr.map((item, index, array) => {
  //   return item + 1
  // })
  // arr.forEach((item, index, array) => {
  //   console.log(1)
  // })
  // return arr.filter((item, index, array) => {
  //   return item > 3
  // })
  // return arr.some((item, index, array) => {
  //   return item > 3
  // })
  // return arr.every((item, index, array) => {
  //   return item > 2
  // })
  // return arr.reduce((prev, next, curr, array) => {
  //   return prev + next
  // }, 4)
  // return arr.reduceRight((prev, next, curr, array) => {
  //   return prev + next
  // })
  // return arr.indexOf(3)
  // return arr.lastIndexOf(3)
  
}

// 
console.log(check(arr))